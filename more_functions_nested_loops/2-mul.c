#include "main.h"
/**
 * mul - function that multiplies two numbers
 * @a: first operand
 * @b: second operand
 *
 * Return: returns mul
*/
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
